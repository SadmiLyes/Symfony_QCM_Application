{% extends 'base.html.twig' %}


{% block body %}

<div class="container bg-light mt-2 card p-4 col-lg-4">
	<h3 class="text-center">
		Sign up
	</h3>
	<hr>
	{{ form_start(form) }}
	    {{ form_widget(form) }}
	    <div class="text-center">
	    	<input type="submit" value="Save" class="btn btn-primary"/>
	    </div>
	{{ form_end(form) }}
</div>
{% endblock %}
