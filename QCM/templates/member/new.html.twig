{% extends 'base.html.twig' %}

{% block title %}New Member{% endblock %}

{% block body %}

	<div class="container bg-light mt-5 card p-4 col-lg-4">
       <h3 class="text-center">
           Create new Member
       </h3>
       <hr>
   <h1>Create new Member</h1>
    {% if error is defined %}
    	<div class="alert alert-danger" role="alert">
 			{{error}}
		</div>
    {% endif %}
    {%   include('member/_form.html.twig') %}
        <div class="text-right">
            <a href="{{ path('member_index') }}" class="btn btn-secondary">back to list</a>
        </div>

   </div>
{% endblock %}