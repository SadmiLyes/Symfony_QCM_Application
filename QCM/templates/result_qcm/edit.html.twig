{% extends 'base.html.twig' %}

{% block title %}Edit ResultQcm{% endblock %}

{% block body %}
    <div class="container bg-light mt-5 card p-4 col-lg-4">
        <h3 class="text-center">
            Edit Result
        </h3>
        <hr>
            {{ include('result_qcm/_form.html.twig', {'button_label': 'Update'}) }}
        <hr>

        <h5 class="mb-3">Delete the result :</h5>
        <div class="mx-auto">
            {{ include('result_qcm/_delete_form.html.twig') }}
        </div>
        <hr>

        <a class="btn btn-secondary" href="{{ path('result_qcm_index') }}">back to list</a>
    </div>



{% endblock %}