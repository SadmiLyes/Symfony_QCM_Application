{% extends 'base.html.twig' %}

{% block title %}Edit Session{% endblock %}

{% block body %}
    <div class="container bg-light mt-5 card p-4 col-lg-4">
    <h3 class="text-center">
        Edit profile
    </h3>
    <hr>

    {{ include('session/_form.html.twig', {'button_label': 'Update'}) }}

        <hr>

        <h5 class="mb-3">Delete the session :</h5>
        <div class="mx-auto">
            {{ include('session/_delete_form.html.twig') }}
        </div>
        <hr>

        <a class="btn btn-secondary" href="{{ path('session_index') }}">back to list</a>

    </div>


{% endblock %}