{% extends 'base.html.twig' %}

{% block title %}ResultQuestion{% endblock %}

{% block body %}
    <h1>ResultQuestion</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ result_question.id }}</td>
            </tr>
            <tr>
                <th>Given</th>
                <td>{{ result_question.given ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('result_question_index') }}">back to list</a>

    <a href="{{ path('result_question_edit', {'id': result_question.id}) }}">edit</a>

    {{ include('result_question/_delete_form.html.twig') }}
{% endblock %}