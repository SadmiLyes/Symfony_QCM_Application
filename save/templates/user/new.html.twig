{% extends 'base.html.twig' %}

{% block title %}New User{% endblock %}

{% form_theme form 'bootstrap_4_layout.html.twig' %}

{% block body %}
    <div class="container mt-4">
        <div class="col-md-11 mx-auto">
            <div class="card " >
                <div class="card-body">
                    <h3 class="card-title text-center text-secondary">Register :</h3>
                    <hr>
                    {{ include('user/_form.html.twig') }}
                    <hr>
                    <a href="{{ path('user_index') }}" class="card-link btn btn-danger">back to list</a>
                </div>
            </div>
        </div>
    </div>
{% endblock %}